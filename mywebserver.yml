---
- hosts: mywebservers
  become: yes 
  tasks: 
  - name: i want to install webserver 
    apt: 
      name: "{{ mywebpackonubuntu= apache2 }}"
      state: starte
    when: ansible_os_family== "Debian"